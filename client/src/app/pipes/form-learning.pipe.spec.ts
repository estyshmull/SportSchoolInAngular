import { FormLearningPipe } from './form-learning.pipe';

describe('FormLearningPipe', () => {
  it('create an instance', () => {
    const pipe = new FormLearningPipe();
    expect(pipe).toBeTruthy();
  });
});
