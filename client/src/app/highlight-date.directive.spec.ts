import { HighlightDateDirective } from './highlight-date.directive';

describe('HighlightDateDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDateDirective();
    expect(directive).toBeTruthy();
  });
});
